language: go
go:
  - 1.11.x
sudo: required

before_install:
  # create /tmp/kingbus for unit test.
  - sudo mkdir /tmp/kingbus/log
  - sudo mkdir /tmp/kingbus/data
  - sudo mkdir /tmp/kingbus/syncer
script:
  - make
  - make test
